# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3         # standard = includes httptools + uvloop

# ── MCP (Anthropic Model Context Protocol SDK) ────────────────────────────────
mcp==1.9.3                        # Anthropic's official MCP Python SDK

# ── AWS / Amazon Bedrock ──────────────────────────────────────────────────────
boto3==1.34.131
botocore==1.34.131                # pinned to match boto3

# ── Datadog observability ─────────────────────────────────────────────────────
ddtrace==2.9.3                    # APM + LLM Observability (auto-instruments boto3)

# ── HTTP client (async-capable, used for Slack + internal calls) ──────────────
httpx==0.28.1                     # async HTTP; also required by FastAPI TestClient
requests==2.32.3                  # sync HTTP (Slack webhook, Datadog API)

# ── Data validation & serialisation ──────────────────────────────────────────
pydantic==2.11.4                  # FastAPI's built-in model layer
pydantic-settings==2.9.1          # env-var config via BaseSettings

# ── Async utilities ────────────────────────────────────────────────────────────
anyio==4.9.0                      # async backend; required by MCP SDK
asyncio-mqtt==0.16.2              # optional: event-bus bridge (can remove if unused)

# ── General utilities ─────────────────────────────────────────────────────────
python-dotenv==1.1.0              # load .env in local dev
structlog==25.3.0                 # structured JSON logging (ECS/Datadog friendly)
tenacity==9.1.2                   # retry decorator for Bedrock / MCP calls
